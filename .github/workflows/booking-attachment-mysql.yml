name: Booking attachment MySQL concurrency

on:
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/booking-attachment-mysql.yml
      - composer.json
      - composer.lock
      - inc/Core/BookingAttachmentService.php
      - inc/Core/BookingAttachmentRepository.php
      - inc/Core/TicketSettlementService.php
      - inc/Core/TicketReconciliationService.php
      - inc/Core/BookingAttachmentPolicy.php
      - inc/Core/BookingInquiryAdmissionService.php
      - inc/Core/BookingLifecycle.php
      - inc/Core/BookingEventSyncService.php
      - inc/Core/BookingEventConversionService.php
      - inc/Core/BookingActivityRepository.php
      - inc/Core/BookingRepository.php
      - inc/Core/BookingSchema.php
      - inc/Core/VenueAuthorization.php
      - inc/Core/VenueMembershipRepository.php
      - phpunit-booking-mysql.xml.dist
      - phpunit-booking-concurrency.xml.dist
      - inc/Abilities/VenueBookingAbilities.php
      - inc/Abilities/VenueBookingEventAbilities.php
      - inc/Abilities/TicketSettlementAbilities.php
      - tests/BookingInquiryAdmissionTest.php
      - tests/MySQLIntegration/BookingAttachmentMySQLIntegrationTest.php
      - tests/MySQLIntegration/BookingEventSyncMySQLIntegrationTest.php
      - tests/MySQLIntegration/BookingSchemaMultisiteTest.php
      - tests/MySQLIntegration/BookingAdmissionConcurrencyMySQLProof.php
      - tests/MySQLIntegration/booking-attachment-mysql-bootstrap.php
      - tests/MySQLIntegration/booking-schema-multisite-bootstrap.php
      - tests/MySQLIntegration/host-wordpress-bootstrap.php
      - tests/MySQLIntegration/wp-tests-config.php

permissions:
  contents: read

jobs:
  booking-attachment-mysql:
    name: Booking attachment MySQL concurrency
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ALLOW_EMPTY_PASSWORD: yes
          MYSQL_DATABASE: wordpress_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping --host=127.0.0.1 --silent"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20

    steps:
      - name: Checkout Extra Chill Events
        uses: actions/checkout@v4

      - name: Checkout Data Machine
        uses: actions/checkout@v4
        with:
          repository: Extra-Chill/data-machine
          ref: main
          path: .ci/data-machine

      - name: Checkout Data Machine Events
        uses: actions/checkout@v4
        with:
          repository: Extra-Chill/data-machine-events
          ref: main
          path: .ci/data-machine-events

      - name: Checkout WP Codebox
        uses: actions/checkout@v4
        with:
          repository: Automattic/wp-codebox
          ref: main
          path: .ci/wp-codebox

      - name: Checkout Homeboy WordPress test adapter
        uses: actions/checkout@v4
        with:
          repository: Extra-Chill/homeboy-extensions
          ref: aee32285109c8d293aec9ed8ddfcd70dd0ff9188
          path: .ci/homeboy-extensions

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install plugin test dependencies
        run: composer install --no-interaction --no-progress --prefer-dist

      - name: Install canonical event dependencies
        run: |
          composer install --working-dir=.ci/data-machine --no-interaction --no-progress --prefer-dist
          composer install --working-dir=.ci/data-machine-events --no-interaction --no-progress --prefer-dist

      - name: Install Homeboy WordPress PHPUnit harness
        working-directory: .ci/homeboy-extensions/wordpress
        run: composer install --no-interaction --no-progress --prefer-dist

      - name: Build WP Codebox
        working-directory: .ci/wp-codebox
        run: |
          set -euo pipefail
          npm install
          npm run build

      - name: Run application-level two-session MySQL proof
        env:
          HOMEBOY_COMPONENT_ID: extrachill-events
          HOMEBOY_COMPONENT_PATH: ${{ github.workspace }}
          HOMEBOY_EXTENSION_PATH: ${{ github.workspace }}/.ci/homeboy-extensions/wordpress
          HOMEBOY_WP_CODEBOX_BIN: ${{ github.workspace }}/.ci/wp-codebox/packages/cli/dist/index.js
          HOMEBOY_WP_CODEBOX_CORE_MODULE: ${{ github.workspace }}/.ci/wp-codebox/packages/runtime-core/dist/index.js
          HOMEBOY_WP_CODEBOX_ARTIFACTS_DIR: ${{ runner.temp }}/booking-attachment-mysql
        run: |
          set -euo pipefail
          export HOMEBOY_SETTINGS_JSON
          HOMEBOY_SETTINGS_JSON=$(jq -nc \
            --arg root "/wordpress/wp-content/plugins/extrachill-events/tests/MySQLIntegration" \
            --arg config "/wordpress/wp-content/plugins/extrachill-events/phpunit-booking-mysql.xml.dist" \
            --arg preload "/wordpress/wp-content/plugins/extrachill-events/tests/MySQLIntegration/booking-attachment-mysql-bootstrap.php" \
            --arg schema_preload "/wordpress/wp-content/plugins/extrachill-events/tests/MySQLIntegration/booking-schema-multisite-bootstrap.php" \
            --arg data_machine_source "$GITHUB_WORKSPACE/.ci/data-machine" \
            --arg data_machine_events_source "$GITHUB_WORKSPACE/.ci/data-machine-events" \
            '{
              database_type: "mysql",
              phpunit_no_tests: "fail",
              wp_codebox_multisite: true,
              wp_codebox_phpunit_test_root: $root,
              wp_codebox_phpunit_config: $config,
              wp_codebox_phpunit_cwd: "/wordpress/wp-content/plugins/extrachill-events",
              wp_codebox_phpunit_preload_files: [$schema_preload, $preload],
              validation_dependencies: [$data_machine_source, $data_machine_events_source]
            }')
          node .ci/homeboy-extensions/wordpress/scripts/test/wp-codebox-phpunit-adapter.mjs --filter='BookingAttachmentMySQLIntegrationTest|BookingEventSyncMySQLIntegrationTest|BookingSchemaMultisiteTest'

      - name: Install native WordPress test runtime
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install --yes subversion
          curl --fail --location --silent --show-error https://wordpress.org/latest.tar.gz | tar -xz -C /tmp
          wp_version=$(php -r "include '/tmp/wordpress/wp-includes/version.php'; echo \$wp_version;")
          wp_branch=$(printf '%s' "$wp_version" | cut -d. -f1,2)
          mkdir -p /tmp/wordpress-tests-lib
          svn export --quiet "https://develop.svn.wordpress.org/tags/${wp_branch}/tests/phpunit/includes" /tmp/wordpress-tests-lib/includes
          svn export --quiet "https://develop.svn.wordpress.org/tags/${wp_branch}/tests/phpunit/data" /tmp/wordpress-tests-lib/data

      - name: Run overlapping application admission proof
        env:
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_USER: root
          DB_PASSWORD: ""
          DB_NAME: wordpress_test
          WP_CORE_DIR: /tmp/wordpress
          WP_TESTS_DIR: /tmp/wordpress-tests-lib
          WP_TESTS_PHPUNIT_POLYFILLS_PATH: ${{ github.workspace }}/vendor/yoast/phpunit-polyfills
        run: |
          set -euo pipefail
          vendor/bin/phpunit -c phpunit-booking-concurrency.xml.dist --filter test_concurrent_exact_inquiry_retry_reuses_one_complete_winner | tee "$RUNNER_TEMP/booking-admission-concurrency.txt"
          grep -E 'OK \(1 test, [1-9][0-9]* assertions?\)' "$RUNNER_TEMP/booking-admission-concurrency.txt"
          if grep -Eiq 'skip|no tests executed|OK, but' "$RUNNER_TEMP/booking-admission-concurrency.txt"; then
            exit 1
          fi

      - name: Verify executed PHPUnit evidence
        env:
          ARTIFACTS_DIR: ${{ runner.temp }}/booking-attachment-mysql
        run: |
          set -euo pipefail
          run_artifacts_dir=$(find "$ARTIFACTS_DIR" -maxdepth 1 -type d -name 'wp-codebox-phpunit.*' -print -quit)
          test -n "$run_artifacts_dir"
          runtime_dir=$(jq -er '.paths.runtimeDirectory' "$run_artifacts_dir/latest-runtime.json")
          commands="$run_artifacts_dir/$runtime_dir/commands.jsonl"
          metadata="$run_artifacts_dir/$runtime_dir/metadata.json"
          test -f "$commands"
          test -f "$metadata"
          jq -ers '
            [.[] | select(.command == "wordpress.phpunit")] as $runs
            | ($runs | length) == 1
              and $runs[0].exitCode == 0
              and $runs[0].result.status == "ok"
              and ($runs[0].stdout | test("OK \\(9 tests, [1-9][0-9]* assertions?\\)"))
              and (($runs[0].stdout | test("skip|no tests executed|OK, but"; "i")) | not)
          ' "$commands" >/dev/null
          jq -e '
            any(.context.managedRuntimeServices[]; .kind == "mysql" and .provider == "docker" and .readiness == "ready")
            and any(.context.recipe.workflow.steps[].args[]; . == "database-type=mysql")
          ' "$metadata" >/dev/null
          jq -rs '[.[] | select(.command == "wordpress.phpunit")][0].stdout' "$commands" > "$ARTIFACTS_DIR/booking-attachment-mysql-phpunit.txt"
          printf '%s\n' "Verified application-level MySQL proof:"
          grep -E 'OK \(9 tests, [1-9][0-9]* assertions?\)' "$ARTIFACTS_DIR/booking-attachment-mysql-phpunit.txt"

      - name: Upload WP Codebox evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: booking-attachment-mysql-evidence
          path: |
            ${{ runner.temp }}/booking-attachment-mysql
            ${{ runner.temp }}/booking-admission-concurrency.txt
          if-no-files-found: error
